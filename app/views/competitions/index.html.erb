<h1>Listing competitions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Points percent flag</th>
      <th>Sets percent flag</th>
      <th>Mvp flag</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @competitions.each do |competition| %>
      <tr>
        <td><%= competition.name %></td>
        <td><%= competition.points_percent_flag %></td>
        <td><%= competition.sets_percent_flag %></td>
        <td><%= competition.mvp_flag %></td>
        <td><%= link_to 'Show', competition %></td>
        <td><%= link_to 'Edit', edit_competition_path(competition) %></td>
        <td><%= link_to 'Destroy', competition, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Competition', new_competition_path %>
